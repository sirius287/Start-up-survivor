function number(value) { return Number(value || 0); }
function marketState(row) { return { category: row.category, trackId: row.track_id || row.category, tick: row.tick, budget: number(row.budget), maxBudget: number(row.max_budget), productPrice: number(row.product_price), marketingSpend: number(row.marketing_spend), targetSegment: row.target_segment, demandIndex: number(row.demand_index), baseDemand: number(row.base_demand), conversionRate: number(row.conversion_rate), baseConversion: number(row.base_conversion), unitsSold: row.units_sold, totalUnitsSold: Number(row.total_units_sold), totalRevenue: number(row.total_revenue), totalCost: number(row.total_cost), netProfit: number(row.net_profit), burnRate: number(row.burn_rate), revenueHistory: row.revenue_history || [0], unitsHistory: row.units_history || [0], demandHistory: row.demand_history || [], conversionHistory: row.conversion_history || [], lastDeployedAt: row.last_deployed_at ? new Date(row.last_deployed_at).getTime() : null, deployCount: row.deploy_count, appliedShocks: row.applied_shocks || [], qualityScore: row.quality_score == null ? null : number(row.quality_score), qualityUpdatedAt: row.quality_updated_at ? new Date(row.quality_updated_at).getTime() : null, shockMultipliers: { demand: 1, budget: 1, conversion: 1, price: 1 },
  // Real economics (SPEC.md §2): the awareness stock, capacity, debt and
  // insolvency state that replace the old "clamp at zero" floor.
  awareness: number(row.awareness), capacity: number(row.capacity), activeCustomers: number(row.active_customers),
  debt: number(row.debt), isInsolvent: Boolean(row.is_insolvent), reputationScar: number(row.reputation_scar) }; }
function team(row) { return { id: String(row.id), teamName: row.team_name, startupName: row.startup_name, category: row.category, trackId: row.track_id || row.category, tagline: row.tagline, isDisqualified: Boolean(row.is_disqualified), disqualifiedReason: row.disqualified_reason || null, joinedAt: new Date(row.joined_at).getTime(), isActive: row.is_active }; }
module.exports = { marketState, team };
